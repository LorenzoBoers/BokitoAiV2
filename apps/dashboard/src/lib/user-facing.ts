/** Shared user-visible fallback labels (EN). */
export const UNKNOWN_LABEL = 'Unknown'

export function unknownErrorMessage(fallback = 'Something went wrong. Please try again.'): string {
  return fallback
}
