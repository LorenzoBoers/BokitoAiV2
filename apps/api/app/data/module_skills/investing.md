# Investing module (prepared, no connections yet)

This module is registered but has no connectors yet. When it ships: positions, quotes, and watchlists via `investing_*` tools; TradingView webhook alerts land as Signals; orders only as `propose_order` decisions. Until then, tell the user investing is coming soon and cannot be queried.
