# Documents module (prepared, no connections yet)

This module is registered but has no connectors yet. When it ships: search and read external document storage via `documents_*` tools; content flows into the existing workspace knowledge stack; uploads only as `propose_upload` decisions. Until then, use the workspace docs tools (`list_docs`, `read_doc`, `search_index`) for knowledge questions.
