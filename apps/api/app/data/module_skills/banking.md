# Banking module (prepared, no connections yet)

This module is registered but has no connectors yet. When it ships: read-only accounts, balances, and transactions via `banking_*` tools; payments only as `propose_payment` decisions. Until then, tell the user banking is coming soon and cannot be queried.
